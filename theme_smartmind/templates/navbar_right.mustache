{{!
    @template theme_smartmind/navbar_right

    SmartMind — Right side navbar.

    Components (edit each independently):
      smartmind/user_area — Search, language, notifications, user menu, edit switch
}}
<nav class="navbar_right d-flex justify-content-end w-100 text-nowrap" aria-label="{{#str}}sitemenubar, admin{{/str}}">
    {{> theme_smartmind/smartmind/user_area }}
</nav>