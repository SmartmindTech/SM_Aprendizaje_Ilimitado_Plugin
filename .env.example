# SmartMind Plugin — Developer Environment Variables
#
# Copy this file to .env and set MOODLE_PATH to your Moodle installation root.
# Then run the setup script (setup.bat on Windows, setup.sh on WSL/Linux/Mac).

# Path to the Moodle installation root (the folder containing admin/, theme/, local/, etc.)
# Windows example:
MOODLE_PATH=C:\MoodleWindowsInstaller-latest-500\server\moodle
# WSL/Linux example:
# MOODLE_PATH=/mnt/c/MoodleWindowsInstaller-latest-500/server/moodle

# Your development branch name (git-pull pulls from dev into this branch)
# Examples: devPaulo, devDiego, devAntonio
GIT_BRANCH=devPaulo

# --- Docker settings (only for docker_linux / docker_windows scripts) ---
# Docker container name running Moodle/IOMAD
DOCKER_CONTAINER=iomad_app
# If docker requires sudo, add your user to the docker group instead:
#   sudo usermod -aG docker $USER
# Then log out and back in. No sudo needed after that.
